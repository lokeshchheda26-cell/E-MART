package com.emart.entity;

/**
 * Delivery choice offered at checkout, per the BRD's Check-Out
 * section ("delivery option (Thru courier or Pick-up from store)").
 */
public enum DeliveryOption {
    COURIER,
    PICKUP
}
